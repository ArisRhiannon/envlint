# Copy to .env and fill in the values. envlint checks .env against this file.
# Comments starting with `# @` declare a schema that envlint validates.
APP_NAME=
# @type port
PORT=3000
# @type url
DATABASE_URL=
